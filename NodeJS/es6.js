//let vs var ---let variable can only be declared once in one block(this is to reduce confusion)
//var variable can be declared multiple times in one block
let name = "John"
var n = "Jane" //this will not throw an error but will change the value of name
console.log(name) //Jane
